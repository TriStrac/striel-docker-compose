version: '3.8'

networks:
  nginx-net:
    external: true

services:
  angular-app:
    build: .
    container_name: scarrowweb-xyz-container
    restart: unless-stopped
    ports:
      - "8080"
    networks:
      - nginx-net

